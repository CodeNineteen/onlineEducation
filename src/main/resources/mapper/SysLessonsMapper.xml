<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.df.mapper.SysLessonsMapper">
    <update id="addDesc">
        update sys_lesson set statu= 1 ,user_id=#{userId} where id=#{rowId}
    </update>
    <update id="delDesc">
        update sys_lesson set statu= 0 ,user_id=#{userId} where id=#{rowId}
    </update>
</mapper>
